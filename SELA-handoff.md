# SELA-handoff.md — 病歷整理器 (Chart)

> **這份是給 Kit Claude 的回流報告。**
> 產生時機：首次對齊 Kit（V7.0.1）

---

## 〇、專案速覽

- **專案名稱：** 病歷整理器 (Chart)
- **專案類型：** 純靜態網頁（單檔 HTML）
- **技術棧：** HTML + CSS + Vanilla JS / html2canvas / jsPDF
- **規模：** 1 個檔案、~1300 行
- **使用 Kit 版本：** V1.8.2
- **完成版本：** V7.0.1
- **完成日期：** 2026-05-08

---

## 一、用 Kit 的整體感受

### 預期外的順利

- 四級分類法（鐵律/建議/順便/不做）很清楚，避免過度對齊破壞既有設計
- claude-init.md 第二章「對齊既有專案 SOP」正好覆蓋這個情境
- favicon 套組直接整合，高清且完整

### 預期外的卡住

- 第一次對齊 Kit 時，Claude 沒有主動讀完規範，需要 SELA 提醒「你是不是沒有認真詳讀」
- 這代表 Kit 的「鐵律 #0」確實需要強化執行

### 對 Kit 的整體評價

- ✓ 對齊既有專案的 SOP 完整，四級分類法有效
- ✓ favicon 套組品質好，整合簡單
- ✗ Claude 執行力需要監督，特別是首次對齊的情境

---

## 二、發現的「跨專案通用坑」（建議進 Kit）

### 強烈建議加坑

#### 1. 靜態 HTML 專案的 localStorage key 版本衝突

- **症狀**：升版後資料消失或讀到舊格式
- **原因**：用版本號動態 key（如 `app_data_v6`），版本一變就讀不到舊資料
- **做法**：固定 key 名稱（如 `app_data`），版本資訊存在 value 內，加 migration 邏輯
- **影響範圍**：所有用 localStorage 的靜態 HTML 專案
- **證據**：本專案 CLAUDE.md 坑 #1
- **檢查 1 結果**：grep Kit「localStorage」→ 無重複

---

## 三、發現的「跨專案設計模式」（建議進 sela-philosophy / 規範）

### 1. 醫療系統的密碼保護模式

- **本案發生情境**：病歷整理器有敏感資料，需要簡易防君子密碼
- **可推廣的原則**：醫療相關專案預設加入簡易密碼保護（sessionStorage 驗證），關閉瀏覽器後需重新輸入
- **代價 / 取捨**：UX 多一步，但符合醫療環境需求
- **建議寫入**：可考慮加入 `tech-stack-lessons.md` 醫療系統章節（如有）

---

## 四、Kit 該瘦身或調整的地方

### Kit 規範修改建議

（本次對齊無具體建議）

### Kit 結構性建議

- 可考慮加入「醫療系統專案」的 reference（從本案萃取），但目前只有一個案例，等更多醫療專案再決定

---

## 五、留在這個專案、**不要回流 Kit** 的東西

- 放腫病歷整理 Prompt v4 整合版（業務邏輯，特定科別）
- 多專科會議 JSON 轉換格式（業務邏輯）
- 影像摘要規範（業務邏輯）
- 預設範本密碼 `Sela1227`（專案設定）
- 開啟密碼 `RTO`（專案設定）
- 病人分頁、檢驗分頁等 UI 結構（業務設計）

---

## 六、Kit Claude 的建議行動清單

### 建議升 Kit 版本

不升版（本次對齊無重大發現需回流）

### 必做

- [ ] 考慮加坑：localStorage key 版本衝突（靜態 HTML 專案）— 等第二個案例確認通用性

### 暫緩

- [ ] 醫療系統 reference — 等更多醫療專案

### 不做

- [ ] 放腫病歷 Prompt 進 Kit（業務邏輯，不通用）
- [ ] 預設範本結構進 Kit（業務邏輯）

---

## 七、給 Kit Claude 的最後備註

這是 Chart 專案首次對齊 Kit，版本號從 V6.15.1 跳到 V7.0.1 是 SELA 決定的（視為大版本里程碑）。

本案特點是「醫療系統 + 純靜態 HTML + localStorage」的組合，目前 Kit 沒有對應 reference。如果未來有更多類似專案，可以考慮從本案萃取 reference。

配色保留 #F97316（非 Kit 預設），因為已在臨床環境驗證多月。這符合 Kit「既有專案不主動改色」的規則。
